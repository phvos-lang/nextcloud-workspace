FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

# node_modules is a named volume that shadows the image's copy, so run install
# on startup to sync any deps added to package.json (no-op when up to date).
CMD ["sh", "-c", "npm install && npm run dev"]
